#include <iostream>
#include <vector>
using namespace std;

int main()
{
	int s, n;
	cin >> s >> n;
	vector<int> v(s, 0);
	
	for (int i = 0; i < n; i++) {
		int num;
		cin >> num;

		for (int j = 0; j < s; j++) {
			if (num == v[j]) {

			}
			else {

			}
		}
	}
}